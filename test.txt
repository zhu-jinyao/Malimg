2023.10.2:
1、
    克隆了代码之后，运行jupyter notebook文件不行，显示原来的.pyc文件有bad magic number。
    解决方法：下载反编译器，用反编译器得到原.py代码文件
2、
    得到文件之后，可以作为库引入，不用加文件后缀。但是运行的时候源文件有一个问题，修改之后，nb文件仍然识别到的是旧文件的信息，输出针对旧.py文件的错误（错的行定位都不变）。
    解决方法：修改完源文件之后，保存；nb文件要restart & run all。
